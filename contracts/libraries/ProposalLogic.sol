library ProposalLogic {
    // function isDeclined(Proposal proposal, uint256 quorum) internal returns(bool) {
    //         return (100 *
    //             (proposal[_proposal].approvalsInfluence + proposal[_proposal].againstInfluence + proposal[_proposal].abstainInfluence)) /
    //             proposal[_proposal].totalInfluence <
    //             quorum;
    // }
}
